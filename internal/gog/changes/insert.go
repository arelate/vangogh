package changes

func Insert(colName string, chg *Change) error {
	//changesCol := mongoClient.Database(mongocl.VangoghDB).Collection(colName + changesSuffix)
	//_, err := changesCol.InsertOne(ctx, chg)
	//return err
	return nil
}
