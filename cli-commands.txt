# Decorators legend:
# $ - supports environmental variable value
# ^ - default property, value
# & - supports multiple values
# * - required value
# {} - placeholder values
# {^} - placeholder values, first value is default

backup

cleanup
    id^&
    slug&
    os&$={operating-systems^}
    lang-code&$={language-codes^}
    download-type&$={download-types^}
    no-patches$
    downloads-layout$={downloads-layouts^}
    all
    test

dehydrate
    id^&
    slug&
    image-type&={image-types}
    all-dehydrated-image-types
    force

generate-missing-checksums
    os&$={operating-systems^}
    lang-code&$={language-codes^}
    download-type&$={download-types^}
    no-patches$
    downloads-layout$={downloads-layouts^}

get-binaries
    os&$={operating-systems^}
    wine
    steamcmd
    all
    force

get-data
    id&
    product-type={all-product-types}
    since-hours-ago
    purchases
    related-api-products
    force

get-description-images
    id&
    since-hours-ago
    all
    force

get-downloads
    id^&
    slug&
    os&$={operating-systems^}
    lang-code&$={language-codes^}
    download-type&$={download-types^}
    no-patches$
    manual-url-filter&
    downloads-layout$={downloads-layouts^}
    update-details
    validate
    checksums-only
    queued
    missing
    all
    debug
    force

get-images
    id^&
    slug&
    missing
    image-type&={image-types}
    all-image-types
    force

get-summary

get-video-metadata
    id^&
    slug&
    missing
    force

import-cookies
    cookies^*

migrate-data
    force

reduce
    product-type={all-product-types}

relayout-downloads
    os&$={operating-systems^}
    lang-code&$={language-codes^}
    download-type&$={download-types^}
    no-patches$
    from*={downloads-layouts}
    to*={downloads-layouts}

serve
    port$*
    stderr$
    os&$
    lang-code$
    no-patches$
    downloads-layout$={downloads-layouts^}

size
    id^&
    slug&
    os&$={operating-systems^}
    lang-code&$={language-codes^}
    download-type&$={download-types^}
    no-patches$
    downloads-layout$={downloads-layouts^}
    missing
    debug
    all

summarize
    since-hours-ago

sync
    since-hours-ago
    os&$={operating-systems^}
    lang-code&$={language-codes^}
    download-type&$={download-types^}
    no-patches$
    downloads-layout$={downloads-layouts^}
    debug$
    no-cleanup
    force
    {sync-options^&}

update-downloads
    os&$={operating-systems^}
    lang-code&$={language-codes^}
    download-type&$={download-types^}
    no-patches$
    downloads-layout$={downloads-layouts^}
    since-hours-ago
    updates-only

users
    create
    delete
    change-password
    list
    username
    password
    new-password
    role

validate
    id^&
    slug&
    os&$={operating-systems^}
    lang-code&$={language-codes^}
    download-type&$={download-types^}
    no-patches$
    downloads-layout$={downloads-layouts^}
    validation-status&={validation-statuses}
    not-valid
    all

version